# 📊 Project Change Log (최근 변경 이력)

이 문서는 `ssh-reports-hub` 서비스 제품군의 **프론트엔드(UI)**, **리포트 분석 엔진(Enricher)**, 그리고 **중앙 스케줄러(Scraper)**의 최근 커밋 분석을 통해 프로젝트의 성장과 기술적 변경 이력을 일목요연하게 정리한 변경이력 파일입니다.

---

## 📅 2026-06-07 (최신 배포 & 고도화)

### 🎨 Frontend (`ssh-reports-hub`)
* **[UI 최적화 및 중복 태그 배지 제거]** `f9e4021`
  * 리포트 아이템 렌더링 시 상위 산업군(`sector`) 및 매칭 종목명(`stock_names`)과 동일한 중복 단어가 태그 배지에 반복 출력되어 레이아웃을 오염시키던 이슈 해결.
  * `tags.filter(t => t !== sector && !stock_names?.includes(t))`를 통한 프론트엔드 최종 세이프티 가드 구축.
* **[AI 요약 다중 엔진 및 DeepSeek/Antigravity 분기 UI]** `06e374d`, `fed78b6`
  * 딥러닝 기반 요약 모델 고도화를 위해 DeepSeek 엔진과 Antigravity 요약 엔진을 사용자가 직접 UI 상에서 선택 및 전환하여 열람할 수 있는 엔진 분기 스위치 탑재.
  * 다른 LLM/에이전트가 쉽게 인계받아 개선할 수 있도록 다중 요약 엔진 통합 가이드 문서 추가 완수.

### 🧠 Tag Engine & Analytics (`ssh-reports-enricher`)
* **[동적 태그 추출기 & 28만 건 DB 정밀 분석 기반 수혈]** `42344ad`, `3fcd24a`
  * 28만 건 운영 DB 타이틀 전수 통계(`unclassified_analysis.json`)를 통해 **99.19%**에 달하던 무분류 누락 원인 완벽 규명.
  * `Overweight`(비중확대), `NDR/미팅`, `YoY/Earnings`(실적 지표), `스몰캡`, `수수/계약`, `턴어라운드`, `컨센서스` 등 12개 이상의 고빈도 금융 전문 테마 정규식 전면 보강.
  * 목표주가/적정주가 타겟 가격 인식 정규식 고도화.
* **[대형주 기반 계층적 산업군 폴백(Fallback) 구조 탑재]** `42344ad`
  * 제목에 산업 키워드가 누락되었더라도 `삼성전자`, `SK하이닉스`, `한미약품`, `현대차` 등 대표 대형주가 검출되면 해당 대표 산업군(반도체, 바이오, 자동차 등)으로 자동 지정 매핑하는 `STOCK_TO_SECTOR` 폴백 사전 연동 및 `_classify_sector` 기능 고도화.
* **[중복 제거 및 AI 오매칭 차단 필터]** `d5601ac`
  * 백엔드 API 단에서 중복 태그를 사전에 드롭하여 반환하도록 동기/비동기 추출 메서드 필터링 보강.
  * `On-Chain` 등 영단어 속에 포함된 `ai` 알파벳을 AI 태그로 오작동 검출하던 버그를 정밀 영문 단어 경계 정규식`(?<![A-Za-z])AI(?![A-Za-z])`으로 완벽히 해결.
* **[안전성 강화 및 최신 분석 최적화]** `d005675`, `63edd99`, `fa36bcd`
  * 미처리 레포트 스캔 순서를 최신순(`DESC`)으로 정렬하여 실시간 수집 리포트가 즉각 태깅을 공급받을 수 있도록 개선.
  * 경로 바인딩 오류 방어 로직 설계.

### 🛡️ Scheduler & Crawler (`ssh-reports-scraper`)
* **[컨테이너 FileNotFoundError (Errno 2) 완벽 방어 패치]** `553bbdc`
  * 도커 컨테이너 격리 환경에서 가상환경 Python이 부재해 `run_fnguide_matcher` 구동 시 에러 추적 로그가 과다 누적되던 장애 요인 규명.
  * 서브프로세스 구동 전 `os.path.exists()`를 이용해 파이썬 실행 파일 및 스크립트 존재 여부를 선제 검증하도록 패치하여 도커 환경 내 장애 요인 완전 진압 (실시간 컨테이너 `exec` 실측 검증 완료).
* **[FnGuide 유사도 매칭 배치 등록 및 API 마이그레이션]** `849d498`, `34cbd4e`
  * FnGuide 요약 리포트 매칭 배치를 중앙 마스터 스케줄러(`scheduler.py`)에 정식 편입.
  * 가상환경 권한 충돌 이슈를 `sys.executable` 및 HTTP 내부 API 호출(Trigger) 분기로 우아하게 다변화.

---

## 📅 2026-06-01 ~ 2026-06-06 (기반 다지기 & FnGuide 고도화)

### 🎨 Frontend & Backend
* **[FnGuide 리포트 가독성 및 UI 필터 극대화]** `70b67fc`, `77296b3`, `6e149c9`
  * 가독성 향상을 위해 폰트 스케일 및 줄간격 조절. 핵심 키워드 하이라이팅 기능 추가.
  * 애널리스트/제공처별 복합 필터 구축 및 인사이트 그룹화 기능 반영.
* **[홈 요약 딥링크 기능 연동]** `9c871db`
  * 홈 화면의 AI 분석 요약 카드를 터치/클릭할 때 FnGuide 디테일 리포트 화면으로 즉각 이동하게끔 딥링크 라우팅 정비.
* **[FnGuide 투자 키워드 사전화 및 스키마 확정]** `ed6cdae`, `9c06802`
  * 공통 인프라 구축을 위해 FnGuide 전용 투자 키워드 메타 사전을 데이터베이스 스키마와 완벽 동기화 완료.

---
**Document Generated by Antigravity AI Engine (Google DeepMind advanced agentic coding team)**
